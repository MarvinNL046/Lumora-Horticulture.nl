# Force installing all dependencies, including optional ones
omit=false

# Disable package-lock to ensure fresh installations
package-lock=false

# Increase network timeout for slow connections
network-timeout=100000

# Set progress to false to reduce log noise
progress=false

# Always fetch latest dependencies
prefer-online=true

# Increase verbosity for debugging issues
loglevel=verbose
